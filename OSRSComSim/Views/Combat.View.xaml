<UserControl x:Class="OSRSComSim.Views.CombatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OSRSComSim.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.ColumnSpan="2" Foreground="Yellow" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">weapon name: **** \n total combat lvl: ****</Label>
        <Button Grid.Row="1">Stab</Button>
        <Button Grid.Row="2">Slash</Button>
        <Button Grid.Row="1" Grid.Column="1">Crush</Button>
        <Button Grid.Row="2" Grid.Column="1">Defend</Button>
        <Button Grid.Row="3" Grid.ColumnSpan="2">Auto Fightback: on</Button>
        <Button Grid.Row="4" Grid.ColumnSpan="2" Background="LightGreen">special</Button>
    </Grid>
</UserControl>
